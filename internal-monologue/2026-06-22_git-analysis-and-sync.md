Reviewed current uncommitted changes and grouped them into two logical commit sets: (1) IDE/build metadata updates for Java 21 and Eclipse/VS Code classpath support in `build.gradle` plus `.settings` facet/compiler files, and (2) `game-service` metrics cleanup in `GameMetrics.java` removing deprecated registry qualifier usage and consolidating counter increment logic. Existing internal notes already document the metrics and classpath fixes, so this summary records the git analysis and planned commit structure before staging, committing, and pushing.