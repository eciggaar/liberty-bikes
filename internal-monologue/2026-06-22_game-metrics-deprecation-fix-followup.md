Updated `game-service/src/main/java/org/libertybikes/game/metric/GameMetrics.java` to remove deprecated `MetricRegistry.Type.APPLICATION` usage. Kept CDI lookup simple by injecting `MetricRegistry` directly and reusing the bean instance for counters and timers. Noted additional issue: previous attempt incorrectly qualified `GameMetrics` bean selection with `@RegistryType`, which applies to `MetricRegistry`, not the bean itself. Remaining reported errors are unrelated workspace classpath/cache issues for `microprofile-telemetry-api-2.0.pom`.